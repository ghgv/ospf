#include <iostream> 
#include <vector> 
#include <cstring>
#include "neighbors.h"
using namespace std;  


/*
	vector<neighbor> a; 
	neighbor uno,dos,tres;
        a.push_back(uno); 
        a.push_back(dos); 
        a.push_back(tres); 


	cout << "Output of begin and end Function: \n"; 
	for (auto i = a.begin(); i != a.end(); ++i) 
		{
		cout << i->name << "\n"; 
		if (strcmp(i->name,"hjhj")==0)
			a.erase(i);
   		}
   
return 0; 
} 
*/
